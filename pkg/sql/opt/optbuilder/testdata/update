exec-ddl
CREATE TABLE jsonb_table (
  j JSONB,
  k JSONB,
  ref TEXT
)
----

# ------------------------------------------------------------------------------
# Basic tests.
# ------------------------------------------------------------------------------

# JSONB single clause update.
build
UPDATE jsonb_table SET j['a'] = '1', k[ref]['b'][0] = '2'
----
error (42804): value type string doesn't match type int of column "ref"

# ------------------------------------------------------------------------------
# Test RETURNING.
# ------------------------------------------------------------------------------

